name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: test (linux)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Library tests
        run: cargo test -p oscar-core -p oscar-mode -p oscar-tools -p oscar-agent --lib --locked

      - name: Build CLI (debug)
        run: cargo build -p oscar-cli --locked

      - name: CLI smoke
        run: |
          set -euo pipefail
          BIN=./target/debug/oscar
          "$BIN" --version
          "$BIN" --help >/dev/null
          # Avoid Rust SIGPIPE/broken-pipe panic when piping to head
          "$BIN" tools list > /tmp/oscar-tools-list.txt
          head -5 /tmp/oscar-tools-list.txt
          "$BIN" tools catalog > /tmp/oscar-tools-catalog.txt
          head -5 /tmp/oscar-tools-catalog.txt
          "$BIN" mcp presets > /tmp/oscar-mcp-presets.txt
          head -5 /tmp/oscar-mcp-presets.txt
          "$BIN" inventory status || true
